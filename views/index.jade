extends layout

block content
  .big-header-thing

    h1
      img(src="/im/pie.png", alt="Tentoring", width="64")
      span.ten Ten
      span toring
    h2.strapline Ten minute mentoring.
    .row
      .large-8.large-offset-2.columns.lefted
        p Mentoring can be time-consuming, but could it be done in ten minute windows?
        p Use Tentoring to give and get advice in the time it takes to make a cup of tea.

  form(method="post", action="/")
    .row
      .large-8.columns.large-offset-2
        .row.collapse
          .small-3.large-2.columns
            label.right(for="email") Email
          .small-9.large-10.columns
            input#email(name="email", type="email", required=true)
        if !login
          .row.collapse
            .small-3.large-2.columns
              label.right(for="name") Name
            .small-9.large-10.columns
              input#name(name="name", type="text", required=true)
          .row.collapse
            .small-3.large-2.columns
              label.right(for="skills") Skills
            .small-9.large-10.columns
              select.chosen(multiple="true",name="skills",data-placeholder="Select your skills")
                each skill in skills
                  option(value=skill)= skill
        .row.collapse
          p &nbsp;
          if login
            p
              input.button.large.expand(type="submit", value="Sign in")
          else
            p
              input.button.large.expand(type="submit", value="Join in!")
          p &nbsp;
          :markdown
            A hack by [Remy](http://twitter.com/rem), [Stef](http://twitter.com/stef) & friends at [Do&nbsp;Startup](http://dolectures.co.uk).
